%h3 Developer
.media
  .profile_photo
    = person_image_link(@dev, :size => :thumb_large, :to => :photos)

  .bd
    =person_link(@dev)

    .info
      = Diaspora::Taggable.format_tags(@dev.profile.tag_string)
